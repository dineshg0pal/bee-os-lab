Operating System:

  # Control + Rules + Protection
  # OS exists to ==> control, protect, share

  # simply OS has:
      *traffic police  - process sceduling
      *translator      - system calls
      *process manager - cpu, ram, disk.


  # flow diagram:

       (user program)
            |
         (kernel)
            |
        (hardware)
